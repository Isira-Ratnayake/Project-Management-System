# Project-Management-System

This is the backend service for the dynamic project and task management system.
It provides simple RESTful APIs for managing tasks, and users, and dynamically adjusts the project timeline based on task updates.